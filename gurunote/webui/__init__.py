"""GuruNote WebView UI package.

PyWebView 기반 새 UI 레이어. 기존 `gurunote.gui` (CTk) 와 공존하며
같은 도메인 모듈(`gurunote.audio`, `.stt`, `.llm`, `.history` 등)을
공유한다.

엔트리포인트: 저장소 루트의 `app_webview.py`.
설계 문서: `docs/webview-ui/ARCHITECTURE.md`.
"""
