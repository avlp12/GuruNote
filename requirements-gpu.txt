# =============================================================================
# GuruNote GPU 로컬 STT 의존성 (Linux/Windows + NVIDIA GPU)
# -----------------------------------------------------------------------------
# 사용 조건:
#   - nvidia-smi 가 감지되는 환경
#   - CUDA PyTorch 가 먼저 설치되어 있어야 함 (setup.sh / setup.bat 가 자동 처리)
#     수동 설치: pip install torch==2.8.0+cu128 torchaudio==2.8.0+cu128 \
#                  --index-url https://download.pytorch.org/whl/cu128
#
# WhisperX:
#   Distil-Whisper + pyannote 화자 분리. 청크 분할 처리로 VRAM ~6GB 에서 안정 동작.
#   화자 분리에 pyannote 사용 (HuggingFace 토큰 + 모델 사용 동의 필요).
#
# whisperx 가 faster-whisper + pyannote 를 의존성으로 자동 설치합니다.
# =============================================================================

whisperx>=3.1
