# =============================================================================
# GuruNote PDF 출력 의존성 (레거시 — v0.7.0.4 부터 requirements.txt 에 통합)
# -----------------------------------------------------------------------------
# 새 설치에서는 `pip install -r requirements.txt` 만으로 PDF 출력에 필요한
# Python 패키지가 모두 설치된다. 이 파일은 기존 자동화 스크립트 호환용으로
# 유지된다.
#
# 시스템 라이브러리(cairo/pango) 는 여전히 수동 설치가 필요할 수 있으며,
# 미설치 시 GUI 의 "Save PDF" 버튼이 설치 확인 다이얼로그를 띄운다:
#   macOS:   brew install cairo pango gdk-pixbuf libffi
#   Ubuntu:  sudo apt install libpango-1.0-0 libpangoft2-1.0-0 fonts-noto-cjk
# =============================================================================

markdown>=3.5
weasyprint>=60
